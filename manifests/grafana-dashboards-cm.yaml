apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-dashboard-scenario1
  namespace: monitoring
  labels:
    grafana_dashboard: "1"
data:
  scenario1-overview.json: |
    {{ lookup('file', 'files/grafana/dashboards/scenario1-overview.json') | indent(4) }}
